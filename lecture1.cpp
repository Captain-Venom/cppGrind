#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int n;
    cin>>n;
}

int main(){
	ios::sync_with_stdio(0);
	cout.tie(0);
	
    //type of initialisation
    // ctype
    int c=2;
    // constructor type
    int con(2);
    // user defined??
    int t{20};
    
	cout <<c<<" "<<con<<" "<<t;
	return 0;
}